// Write a function that merges two sorted arrays

const arryA = [2, 3, 6, 9, 13]
const arryB = [1, 4, 5, 7, 12]

const merge = (left, right) => {
  let output = []

  while (left.length && right.length) {
    
  }


  return output
}

const resp = merge(arryA, arryB)
console.log(resp)
